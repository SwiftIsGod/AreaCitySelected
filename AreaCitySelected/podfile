# Uncomment this line to define a global platform for your project
  platform :ios, '8.0'

target 'AreaCitySelected' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for AreaCitySelected
     pod 'SelectedPicker', '~> 1.0'

  target 'AreaCitySelectedTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AreaCitySelectedUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
